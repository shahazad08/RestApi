#
# from django.conf.urls import url   # A function that takes a prefix, and an arbitrary number of URL patterns,
# # and returns a list of URL patterns in the format Django needs.
# from django.urls import path
#
# from .views import CreateUserAPIView   # Importing Views that are define in a views.py
# from . import views
#
# urlpatterns = [
#     # path('/user1', views.home, name='home'),         # For a home page
#     url(r'^createnotes/$', CreateUserAPIView.as_view()),  # For a Register Page
#     # url(r'^obtain_token/$', LoginView.as_view()),    # For a Login Page
#
# ]